// Server-side Supabase client placeholder
// In a Vite SPA, there is no server-side. Server operations are handled
// via Vercel serverless functions (in /api directory at project root).
// This module re-exports the client for code that imports from here.

export { supabase as supabaseAdmin } from './client';
